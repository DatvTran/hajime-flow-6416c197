# Copy to .env in this folder (Vite loads it). Never commit .env.
# Get keys from https://dashboard.stripe.com/apikeys — use test keys locally when possible.

VITE_STRIPE_PUBLISHABLE_KEY=

# Optional: if the Stripe API is not proxied on the same origin in production
# VITE_API_BASE_URL=
